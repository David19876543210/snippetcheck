{
  "name": "snippetcheck-workspace",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev:web": "pnpm --filter snippetcheck-web dev",
    "dev:cli": "pnpm --filter snippetcheck dev"
  },
  "packageManager": "pnpm@10.34.5"
}
